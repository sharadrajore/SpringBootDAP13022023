spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coupon-service
    username: root
    password: root
  jpa:
   hibernate:
    ddl-auto: update
  application:
    name: coupon-service
    
eureka:
  client:
    register-with-eureka: true
    
    fetch-registry: true
    
    
    service-url:
      defaultZone: http://localhost:8761/eureka
      
server:
  port: 8085
  
 
    
    
    

  

    
 